apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: communicator-server-management-service
  namespace: communicator-prod
  annotations:
    konghq.com/protocols: https
    konghq.com/https-redirect-status-code: '308'
    konghq.com/plugins: communicator-prod-auth0-jwt,communicator-prod-cors
spec:
  tls:
    - hosts:
        - prod.server-management-service.communicator.kamilwronka.com
      secretName: communicator-prod-tls
  ingressClassName: kong
  rules:
    - host: prod.server-management-service.communicator.kamilwronka.com
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: communicator-server-management-service
                port:
                  number: 4000
